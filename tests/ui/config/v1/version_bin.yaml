crates:
- name: A
cases:
- cmd: run default
  stderr: |
    thread 'main' panicked at 'Invalid config file: "Config file version is too new. The file requires at least 0.100.0, but the current version is 0.13.3. Please update Zepter or ignore this check with `--check-cfg-compatibility=off`."', src/cmd/run.rs:27:46
    note: run with `RUST_BACKTRACE=1` environment variable to display a backtrace
  code: 101
configs:
- to_path: .zepter.yaml
  from_path: null
  verbatim: |
    version:
      format: 1
      binary: 0.100.0

    workflows:
      default:
        - [ ]
no_default_args: true
