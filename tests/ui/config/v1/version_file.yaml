crates:
- name: A
cases:
- cmd: run default
  stderr: |
    thread 'main' panicked at 'Invalid config file: "Only format version '1' is currently supported."', src/cmd/run.rs:27:46
    note: run with `RUST_BACKTRACE=1` environment variable to display a backtrace
  code: 101
configs:
- to_path: .zepter.yaml
  from_path: null
  verbatim: |
    version:
      format: 1.2
      binary: 0.0.0

    workflows:
      default:
        - [ ]
no_default_args: true
